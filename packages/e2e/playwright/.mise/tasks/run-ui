#!/usr/bin/env bash
#MISE description="Open Playwright UI mode (time-travel debugger, watch mode)"
#MISE depends=[":install-browsers"]
#MISE env={ NODE_OPTIONS = "--dns-result-order=ipv4first" }
set -euo pipefail
pnpm exec playwright test --ui "$@"
