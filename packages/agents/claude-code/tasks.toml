["image:agent:claude-code"]
description = "Build the claude-code agent image"
dir = "{{config_root}}"
depends = ["image:platform-base"]
run = '''
#!/usr/bin/env bash
[ -n "${SKIP_IMAGE_BUILD:-}" ] && exit 0
docker build -t platform-claude-code:latest packages/agents/claude-code
'''
