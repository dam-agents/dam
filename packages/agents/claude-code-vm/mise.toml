[tasks.image]
description = "Build the claude-code-vm containerDisk (bootc → qcow2, hash-cached) from the claude-code image"
dir = "{{vars.repo_root}}"
depends = ["//packages/agents/claude-code:image"]
run = "packages/agents/claude-code-vm/build.sh"
