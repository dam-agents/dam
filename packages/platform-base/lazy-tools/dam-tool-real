#!/bin/sh
# Resolve a command ignoring the lazy-tools directory, so a shim can exec the
# real binary without re-entering itself.
PATH=$(printf '%s' "$PATH" | tr ':' '\n' | grep -vx /usr/local/share/lazy-tools | paste -sd:) \
    command -v "$1"
