#!/usr/bin/env bash
#MISE dir="{{vars.repo_root}}"
[ -n "${SKIP_IMAGE_BUILD:-}" ] && exit 0
docker build -t platform-controller:latest packages/controller/
docker build -t platform-vm-runner:latest --build-context vm-runner=packages/vm-runner -f packages/controller/Dockerfile.vm-runner packages/controller/
