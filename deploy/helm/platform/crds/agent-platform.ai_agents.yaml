# Code generated by controller-gen. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    agent-platform.ai/crd-schema-generation: "9"
    controller-gen.kubebuilder.io/version: v0.21.0
    helm.sh/resource-policy: keep
  name: agents.agent-platform.ai
spec:
  group: agent-platform.ai
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    shortNames:
    - agt
    singular: agent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .spec.image
      name: Image
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              agentHome:
                type: string
              backend:
                properties:
                  type:
                    enum:
                    - container
                    - vm
                    type: string
                  vm:
                    type: object
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: 'vm block requires type: vm'
                  rule: self.type == 'vm' || !has(self.vm)
              description:
                type: string
              env:
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              grantedConnectionIds:
                items:
                  type: string
                type: array
              grantedSecretIds:
                items:
                  type: string
                type: array
              hibernationTimeout:
                type: string
              image:
                type: string
              imagePullPolicy:
                type: string
              imagePullSecretRef:
                type: string
              init:
                type: string
              l7Hosts:
                items:
                  maxLength: 253
                  pattern: ^(\*\.)?([a-zA-Z0-9]([-a-zA-Z0-9]{0,61}[a-zA-Z0-9])?)(\.[a-zA-Z0-9]([-a-zA-Z0-9]{0,61}[a-zA-Z0-9])?)*$
                  type: string
                maxItems: 256
                type: array
              mounts:
                items:
                  properties:
                    path:
                      type: string
                    persist:
                      type: boolean
                    size:
                      type: string
                  required:
                  - path
                  - persist
                  type: object
                type: array
              name:
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              resources:
                properties:
                  limits:
                    additionalProperties:
                      type: string
                    type: object
                  requests:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              runtimeClassName:
                type: string
              secretRef:
                type: string
              storageClass:
                type: string
              storageSize:
                type: string
              telemetryAttributionId:
                type: string
            required:
            - image
            type: object
            x-kubernetes-validations:
            - message: runtimeClassName selects a container runtime and is invalid
                on the vm backend
              rule: '!has(self.backend) || self.backend.type != ''vm'' || !has(self.runtimeClassName)'
            - message: secretRef (envFrom projection) is not supported on the vm backend
              rule: '!has(self.backend) || self.backend.type != ''vm'' || !has(self.secretRef)'
          status:
            properties:
              agentPodRestartReason:
                type: string
              agentPodRestarts:
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
