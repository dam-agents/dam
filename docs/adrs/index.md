<!-- generated by scripts/adr-index.mjs, do not edit — run `mise run docs:generate:adr-index` -->

# ADR Index

Generated projection of the ADR log. Read this first when authoring a new decision: scan the summaries, then open only the records that matter. Superseded status is derived from `supersedes` links, so an old record always shows its trajectory.

## Accepted

| ID | Title | Supersedes | Subsystem | Summary |
| --- | --- | --- | --- | --- |
| 001 | [Ephemeral containers + persistent workspace volumes](001-ephemeral-containers.md) |  | agent-lifecycle | Fresh container per agent invocation, with workspace and session state persisted on mounted volumes and system packages baked into the image. |
| 002 | [Memory — platform provides primitives, agents own semantics](002-memory-primitives.md) |  | persistence | Platform provides persistent, isolated file storage; agents design their own memory formats, consolidation, and search. |
| 003 | [Kubernetes from the start — k3s for local dev, K8s for production](003-k8s-from-the-start.md) |  | platform-topology | Build on Kubernetes from day one using k3s via lima locally and the same Helm chart in production, skipping a Docker-compose phase. |
| 004 | [ACP over A2A for the experiment](004-acp-over-a2a.md) |  | connections | Adopt Zed's ACP instead of A2A for the greenfield experiment; reconcile with A2A later if needed. |
| 005 | [Gateway pattern for credentials — agent never sees tokens](005-credential-gateway.md) |  | security-and-credentials | Route all external service calls through a credential-injecting gateway so the agent never holds tokens, with per-service policy, audit logging, and human-in-the-loop approval. |
| 006 | [ConfigMaps over CRDs — namespace-scoped resource model](006-configmaps-over-crds.md) |  | platform-topology | Model agent templates, instances, and schedules as labeled ConfigMaps instead of CRDs so the platform deploys with namespace-scoped RBAC and no cluster-admin. |
| 007 | [ACP traffic always proxied through the API Server](007-acp-relay.md) |  | connections | Relay all ACP WebSocket traffic through the API Server rather than direct UI-to-pod connections, enabling network isolation and wake-on-connect. |
| 008 | [Controller-owned cron with exec-based trigger delivery](008-trigger-files.md) |  | agent-lifecycle | Controller owns cron scheduling and delivers triggers by writing JSON files into the pod via kubectl exec, which agent-runtime watches to start ACP sessions. |
| 009 | [Go for Controller, TypeScript for API Server](009-go-and-typescript.md) |  | platform-topology | Write the Controller in Go for client-go and operator tooling, and the API Server in TypeScript to share toolchain and types with the React UI. |
| 012 | [Runtime lifetime — single-use Jobs](012-runtime-lifetime.md) |  | agent-lifecycle | Run each agent turn as a single-use Kubernetes Job instead of keep-alive pods, backed by a read cache and two-tier per-session and shared volumes. |
| 013 | [UI approach — chat-primary, dashboard for inspection](013-ui-approach.md) |  | agent-lifecycle | Make chat the primary interaction surface with a dashboard for inspection and override, built as a custom React UI; Slack is the first external channel. |
| 015 | [Multi-user authentication via Keycloak + OneCLI fork with token exchange](015-multi-user-auth.md) |  | security-and-credentials | Authenticate users through Keycloak OIDC and scope K8s resources by an owner label, with per-user credentials brokered by the API server. |
| 016 | [Messenger integration handled by API Server](016-messenger-integration.md) |  | channels | Let the API Server host messenger integrations through a pluggable channel adapter interface, waking hibernated instances and opening a fresh ACP session per mention. |
| 017 | [DB-backed ACP sessions for metadata](017-db-backed-sessions.md) |  | persistence | Add a PostgreSQL sessions table as the source of truth for session existence and metadata, enriched with live ACP data at query time. |
| 018 | [Slack integration — Socket Mode, channel-based routing, identity linking](018-slack-integration.md) |  | channels | Connect the Slack app over Socket Mode, link users to Keycloak via a slash command, and gate access by channel membership plus per-instance allowlist. |
| 020 | [Responsive mobile UI, ACP session controls, PWA](020-responsive-ui-pwa.md) |  | agent-lifecycle | Make the chat UI responsive for mobile, expose ACP session controls driven dynamically from session state, and ship a PWA. |
| 021 | [Slack outbound messaging — MCP tool](021-slack-outbound.md) |  | channels | Expose an MCP tool so agents post outbound Slack messages through the API Server and SlackWorker, using a fire-and-forget threading model. |
| 022 | [Harness API server — separate port with restricted API surface](022-harness-api-server.md) |  | agent-lifecycle | Run a second HTTP server on port 4001 in the API server process exposing only trigger and MCP endpoints reachable by agent pods. |
| 023 | [Harness-agnostic agent base image](023-harness-agnostic-base-image.md) |  | agent-lifecycle | Ship a single platform-base image owning ACP relay, triggers, and file service; concrete agents extend it and drop two fixed harness scripts. |
| 024 | [Connector-declared pod envs + per-agent env overrides](024-connector-declared-envs.md) |  | connections | Derive pod env vars from connector-declared mappings and per-agent overrides, resolved last-occurrence-wins so the credential owner names its own envs. |
| 026 | [Persistent ACP sessions via per-session log and cursor fan-out](026-session-log-replay.md) |  | persistence | Treat each session as a runtime-owned append-only log with per-channel cursors, fanning events to all engaged clients and serving session/load from memory. |
| 028 | [Configurable injection on generic secrets](028-generic-secret-injection-config.md) |  | security-and-credentials | Generic secrets gain optional pathPattern scoping and an injectionConfig header/value override so non-standard providers need no per-provider code. |
| 029 | [Per-instance messenger channels](029-per-instance-channels.md) |  | channels | Per-instance channels like Telegram store bot tokens in k8s Secrets, authorize per-conversation via /login, and hold one ACP session per authorized thread. |
| 030 | [Skills — connectable sources and install](030-skills-marketplace.md) | 011 | skills | Skills are transported from external connectable git sources (user, system, template) into instance PVCs, with publish via GitHub REST through the credential gateway. |
| 031 | [Schedules use RRULE for includes and structured quiet hours for exclusions](031-schedule-rrule-quiet-hours.md) |  | agent-lifecycle | Schedules express recurrence with RFC 5545 RRULE and exclusions with structured time-of-day quiet-hours windows, both timezone-aware. |
| 033 | [Envoy-based credential gateway with ext_authz HITL — drop OneCLI](033-envoy-credential-gateway.md) | 010 | security-and-credentials | Replace OneCLI with a per-pod Envoy sidecar for credential injection and an API-Server ext_authz service for human-in-the-loop approval. |
| 035 | [Unified HITL UX — verdict authority outside the agent pod](035-unified-hitl-ux.md) |  | security-and-credentials | HITL approvals surface as one ACP request_permission primitive with verdict authority in the API Server, backed by Postgres egress_rules driving L4/L7 ext_authz network gating. |
| 036 | [Redis as a platform primitive — pub/sub, queues, cache](036-redis-platform-primitive.md) |  | platform-topology | Adopt Redis alongside Postgres for cross-replica pub/sub, simple queues, and shared cache, keeping Postgres as the source of truth. |
| 037 | [Remote terminal — split "chat" and "terminal" session modes](037-remote-terminal.md) |  | agent-lifecycle | Sessions gain a chat (ACP) and a terminal (TUI-over-PTY) mode, delivered first as a web terminal emulator rendering a real harness TUI in the agent pod. |
| 038 | [Paired agent and gateway pods — cluster-enforced credential boundary](038-paired-gateway-pod.md) |  | security-and-credentials | Run separate agent and gateway pods per instance so NetworkPolicy structurally forces all agent egress through the gateway. |
| 039 | [Platform CLI foundation — TypeScript on Node, npm distribution](039-cli-foundation.md) |  | cli | The CLI is a TypeScript Node package distributed via npm, sharing the tRPC contract, with XDG-based config and a server-advertised version floor. |
| 040 | [Unified secret contributions — controller-merged at render time](040-unified-secret-contributions.md) |  | connections | A secret's host/path and env mappings flow through one server-orchestrated fanout; the controller merges envs into the pod at render time, with user-typed envs winning collisions. |
| 041 | [Istio ambient mesh — SPIFFE identity for every internal hop](041-istio-ambient-mesh.md) |  | platform-topology | Adopt Istio ambient mesh with a per-instance ServiceAccount as the single SPIFFE identity, replacing the pair-key NetworkPolicy, the pod-IP resolver, and the trusted instance header. |
| 042 | [Agent egress is gated by NetworkPolicy; the agent is not a mesh participant](042-agent-egress-network-policy.md) |  | platform-topology | Take the agent pod out of the mesh and gate its egress with a per-pair kernel NetworkPolicy admitting only cluster DNS and the paired gateway pod. |
| 043 | [Three-layer agent pod configuration](043-agent-pod-config-layers.md) |  | agent-lifecycle | Split agent pod config into chart-only base platform policy, chart-wide template defaults, and per-template overrides, with the controller falling back per field at reconcile. |
| 044 | [Provider twin secrets — multiple injection points per credential](044-provider-twin-secrets.md) |  | connections | A provider preset declares extra wire injection points; the api-server fans one credential across linked twin Secrets and cascades every lifecycle op, keeping it one card to the user. |
| 045 | [File import — bundled, top-level replace into the work dir](045-file-import.md) |  | agent-lifecycle | Imports are a one-shot bundled tar streamed through api-server that top-level-replaces same-named entries into the agent's work dir, extracted by agent-runtime. |
| 047 | [Terms of Use acceptance — api-server gate with hash-as-proof](047-terms-of-use-acceptance.md) |  | security-and-credentials | An api-server middleware returns HTTP 412 on every JWT-authenticated route until the sub accepts the current Helm-set Terms Version, storing version plus sha256 text hash in Postgres. |
| 048 | [Usage tracking — append-only activity log with pseudonymized identifiers](048-usage-tracking.md) |  | usage-tracking | An append-only Postgres table records activity read through named SQL views, HMAC-pseudonymizes the Keycloak sub at the repository boundary, and gates reads with a platform-inspector role. |
| 049 | [Lazy per-directory workspace fetch](049-lazy-workspace-fetch.md) |  | agent-lifecycle | The Workspace panel fetches only expanded directories via one batched per-poll request with independent per-directory failures, holding expansion state per-agent in page-session memory. |
| 050 | [Platform-reserved paths](050-platform-reserved-paths.md) |  | agent-lifecycle | The agent-runtime file API reserves only .triggers/ and .initialized from listing and writes; all other previously noise-filtered paths become listable and writable. |
| 051 | [Connections, Connection Templates, and Contributions — unified configuration model](051-connections-and-contributions.md) |  | connections | Replace the OAuth-app and provider-preset registries with one model where every integration is a Connection built from a Connection Template that emits a typed Contribution set when granted. |
| 055 | [Agent-owned session metadata via ACP `_meta`; no server-side session store](055-agent-owned-session-metadata.md) |  | agent-lifecycle | Drop the Postgres sessions table; the agent-runtime owns session metadata and every reader gets it from ACP session/list. |
| 056 | [Browser-driven E2E tracer with values-gated test affordances](056-browser-driven-e2e.md) | 014 | platform-topology | Playwright browser-driven tests replace API-level vitest e2e, running against a fresh cluster with test affordances gated behind e2e.* Helm values. |
| 057 | [Structured logging for the api-server](057-structured-logging.md) |  | logging | The api-server emits one-JSON-line-per-record Pino logs on stdout, first used for a real-identity security audit trail at standard log levels. |
| 058 | [CRDs over ConfigMaps — reconciled resources become custom resources](058-crds-over-configmaps.md) |  | platform-topology | Migrate reconciled Agent and Fork resources from labeled ConfigMaps to CRDs with a status subresource; Templates stay ConfigMaps. |
| 059 | [Agent readiness is controller-computed status — agent ∧ gateway](059-agent-readiness-status.md) |  | agent-lifecycle | The controller computes readiness as agent-and-gateway pod Ready gated on the rolled-out revision and publishes it on Agent status; the api-server reads only that and drops all pod access. |
| 060 | [Unified runtime-channel apply path + settlement tracking](060-unified-apply-path-and-contributions-settled-gate.md) |  | platform-topology | Apply Contributions out-of-band from a single background worker that dispatches only to Ready agents and tracks per-version settlement for retry. |
| 061 | [Warm PVC pool for instant agent workspace provisioning](061-warm-pvc-pool.md) |  | persistence | The controller keeps size-keyed pools of pre-provisioned bound volumes on an immediate-binding class; new agents claim a spare at create or fall back to dynamic provisioning. |
| 062 | [SSH access to agents via an in-pod inetd sshd tunneled over the agent WebSocket](062-ssh-access.md) |  | security-and-credentials | Run a per-connection inetd sshd inside the agent pod and tunnel raw SSH bytes over a new /api/ssh WebSocket reusing the existing upgrade auth. |
| 063 | [Generated table migrations, hand-written views, squashed baseline](063-hand-written-migrations.md) |  | persistence | Table/index/enum migrations are generated from the schema, reporting views are hand-written SQL, history is squashed to a baseline, guarded by a database-free snapshot check. |
| 064 | [Slack E2E — Bolt behind a swappable port, fork path exercised end-to-end](064-slack-e2e-bolt-port.md) |  | channels | Hide the Bolt SDK behind a first-party Slack port so e2e wires an in-process fake while the fork path runs real Jobs and authenticated egress end-to-end. |
| 065 | [Shared CRDs across co-located environments](065-shared-crds-multi-env.md) |  | platform-topology | Co-located environments share one CRD set installed by the newest release; a CI gate blocks schema changes that break an environment on an older release. |
| 066 | [Pod services — image-provided background processes supervised by agent-runtime](066-pod-service-supervision.md) |  | agent-lifecycle | Agent images may ship one optional pod-service executable that agent-runtime supervises for the pod lifetime, with capped-backoff crash restart and SIGHUP env reload. |
| 067 | [Local model gateway fronts custom Anthropic upstreams for claude-code](067-local-model-gateway.md) |  | security-and-credentials | claude-code fronts custom Anthropic upstreams with a loopback passthrough gateway that serves the live catalog under a discovery-accepted prefix and maps names back to upstream ids per request. |
| 068 | [Frozen shared CRDs — operator-applied schema upgrades](068-operator-applied-crds.md) |  | platform-topology | Shared CRDs ship in the chart crds/ dir and are never touched on upgrade; schema changes happen only through an operator-run script that verifies live-cluster backwards-compatibility before applying. |
| 069 | [Credential env via the runtime channel — injected at harness spawn, not baked into the pod](069-runtime-env-injection.md) |  | agent-lifecycle | Credential-placeholder env rides the runtime channel and merges into the harness environment at spawn, so grant changes respawn the harness instead of rolling the pod. |
| 071 | [Postgres role separation — no SUPERUSER on app roles](071-postgres-role-separation.md) |  | persistence | Bundled Postgres runs two NOSUPERUSER per-service database owners plus one reserved SUPERUSER for DBA work, replacing the single shared superuser role. |
| 072 | [Gateway credential injection for gRPC hosts](072-grpc-credential-injection.md) |  | security-and-credentials | A host's terminating chain can opt into HTTP/2 so gateway credential injection covers gRPC streams, with only the secret credential half injected and the non-secret half allowed as plain pod env. |
| 073 | [Per-template scheduling for runtimeClassName and nodeSelector](073-per-template-scheduling.md) |  | agent-lifecycle | runtimeClassName and nodeSelector become per-template overridable agent-spec fields while all other scheduling stays chart-wide. |
| 074 | [Unified `drivers:` manifest with pluggable events and default-on built-ins](074-unified-drivers.md) |  | agent-lifecycle | The runtime manifest folds contribution and event kinds into one drivers map resolved through the plugin registry, with built-ins on by default and event handlers now externally overridable. |
| 075 | [Periodic work on BullMQ](075-periodic-jobs-bullmq.md) |  | api-server | Recurring reconciliation ticks run as BullMQ job schedulers on per-job queues instead of per-replica setInterval loops. |
| 077 | [The spawn primitive is an ephemeral Agent; lifecycle and result contract are separate concerns](077-sandbox-as-ephemeral-agent.md) |  | agent-lifecycle | A spawnable node is a normal ephemeral Agent a driver creates, prompts once, and polls; its autosweep lifecycle lives on the Agent (Sweepable + Agent Sweep) and its run-once result contract lives in a separate Invocations context. |
| 078 | [Invocation egress follows the driver (Egress Aliasing)](078-invocation-egress-aliasing.md) |  | security-and-credentials | The ext_authz gate resolves an Invocation target to its driver before every egress decision; the target has no egress identity of its own, and deleting a driver cascades to its targets. |
| 079 | [One channel access model — the binding is the authorization](079-single-channel-access-model.md) | 027, 076 | channels | Per-person channel access (identity linking, allow-lists, per-turn impersonation forks) is removed; every binding lends the Agent to the conversation and every turn runs as the Agent, attributed to the messenger-native sender. |
| 080 | [Agent workspaces on ReadWriteOnce storage, migrated in place](080-rwo-workspace-storage.md) |  | persistence | Workspace volumes are ReadWriteOnce on ordinary storage; a controller-run, checksum-verified migration drains existing RWX volumes; the bundled NFS server is deprecated and dam-run is disabled until it has a non-shared-workspace model. |
| 081 | [Admit a blocked start by hibernating the owner's idle agents early](081-admit-by-hibernation.md) |  | budgets | The per-user budget gate may admit an over-budget start by hibernating that owner's unattended idle agents ahead of their timeout, longest-idle first, instead of refusing until the user frees room by hand. |
| 082 | [Prompt delivery is measured by the server, not inferred by the client](082-server-authoritative-prompt-delivery.md) |  | agent-lifecycle | The runtime tells the originating channel what happened to each identified prompt via two ephemeral notifications, and the client's delivery indicator is a state machine driven only by those frames. |
| 083 | [Eventing layers by guarantee — in-process sagas, Redis signals, BullMQ work](083-eventing-layering.md) |  | platform-topology | Domain events stay in-process and non-durable, consumed by sagas that forward cross-replica signals to Redis pub/sub or enqueue durable work as BullMQ jobs; no single distributed bus. |
| 084 | [A resumed channel thread session is caught up on what it could not see](084-resumed-session-catch-up.md) | 025 | channels | A resuming channel thread turn carries the messages that arrived after that Agent's own last turn, best-effort, because a session's context only covers the messages that were relayed to it. |
| 085 | [Usage passthrough access via a reconciled group role](085-usage-passthrough-grants.md) |  | usage-tracking | A credential-less group role holds SELECT on the usage source passthrough views, reconciled by the api-server after each migration run rather than granted inside migrations, with operators granting membership; the startup path it rides on is made safe for concurrent replicas. |
| 086 | [Pod-owned live updates over the agent's own tRPC surface](086-pod-owned-live-updates.md) |  | platform-topology | Pod-owned state is read and watched over the agent-runtime's own tRPC surface relayed over WebSocket; a Watch lives only as long as its subscriber and emits topic-plus-ids notices, never state. |
| 087 | [Knowledge-base sharing via a published read-only MCP snapshot](087-knowledge-base-mcp-share.md) |  | knowledge-bases | Serve knowledge-base shares from a published object-store snapshot over a read-only aggregate MCP endpoint consumed in-cluster, not by proxying the live agent. |

## Superseded

| ID | Title | Superseded by | Subsystem | Summary |
| --- | --- | --- | --- | --- |
| 010 | [OneCLI deployment — single image, two Services, platform-managed lifecycle](010-onecli-deployment.md) | 033 | security-and-credentials | Deploy OneCLI as one Deployment exposing separate gateway and web Services, backed by cert-manager CA and PostgreSQL, with Controller-managed tokens. |
| 011 | [Skills via Claude plugin marketplace](011-skills-claude-marketplace.md) | 030 | skills | Source and manage skills through Claude's plugin marketplace rather than cross-harness registries or git-committed files. |
| 014 | [E2E integration testing against dedicated k3s cluster](014-integration-testing.md) | 056 | platform-topology | Run full-stack end-to-end tests against a dedicated ephemeral k3s cluster provisioned via mise, isolated from the dev cluster. |
| 025 | [Persistent ACP session per Slack thread](025-thread-session.md) | 084 | channels | Persist one ACP session per Slack thread, mapped by (instanceId, threadTs) in PostgreSQL and resumed on follow-ups, with fallback to a fresh session. |
| 027 | [Slack per-turn user impersonation — foreign repliers fork the instance into a K8s Job](027-slack-user-impersonation.md) | 079 | channels | Foreign Slack repliers run their turn in a per-turn Kubernetes Job carrying their own credentials, sharing the instance PVC via RWX. |
| 076 | [Per-channel access modes — shared and person-scoped](076-channel-access-modes.md) | 079 | channels | Each channel binding picks its access mode at bind time — shared (place-scoped, Agent credentials, open speaker set) or person-scoped (identity linking, allow-list, per-turn forks). |

## Deprecated

_None._

## Drafts

| ID | Title | Supersedes | Subsystem | Summary |
| --- | --- | --- | --- | --- |
| 019 | [Scheduled session identity and lifecycle](019-session-identity.md) |  | persistence | Give each cron schedule its own persistent ACP session resumed on every fire, tracked by scheduleId in the DB sessions table. |
| 032 | [Centralized pod-reachability primitive; observed pod Ready is the truth](032-pod-reachability-primitive.md) |  | agent-lifecycle | Every caller reaches an agent pod through one single-flight ensureReady(instanceId) primitive that treats observed pod Ready, not desiredState, as authoritative. |
| 034 | [Push declarative file state to agent pods](034-pod-files-push.md) |  | agent-lifecycle | agent-runtime materializes declarative file fragments from opaque producers into agent pods over SSE, merging without a pod restart. |
| 046 | [Eliminate Instance — collapse into Agent](046-eliminate-instance.md) |  | agent-lifecycle | Collapse Instance into Agent so one ConfigMap carries definition, runtime state, and lifecycle; Template, Agent, and Fork become the only durable concepts. |
| 052 | [Unified runtime channel — state snapshot plus event stream between api-server and agent-runtime](052-runtime-channel.md) |  | channels | Replace pod-files SSE, exec trigger drops, and direct skills tRPC with one tRPC channel carrying a desired-state snapshot and an ordered event stream that share a single per-agent version cursor. |
| 053 | [Transactional outbox + BullMQ worker for runtime-channel delivery](053-runtime-outbox-worker.md) |  | channels | Agent-bound state changes commit to a Postgres outbox and a per-replica BullMQ worker dispatches them over the runtime channel. |
| 054 | [Branded Keycloak login via Keycloakify-built custom image](054-keycloak-theme.md) |  | security-and-credentials | Ship a first-party Keycloak image with a Keycloakify-built React theme baked in, always enabled via loginTheme=platform. |
| 070 | [API keys with scopes for headless CLI use](070-api-keys-headless-auth.md) |  | security-and-credentials | Long-lived owner-scoped API keys with a pk_ prefix share the bearer slot with Keycloak JWTs, carrying a fixed five-scope vocabulary and an agent binding. |
| DRAFT | [Agent reads served from a watch-backed cache, writes from live reads](DRAFT-agent-read-cache.md) |  | agent-lifecycle | The api-server serves Agent reads from a per-replica watch-backed cache, while any reader that decides a write from what it read stays on a live read. |
| DRAFT | [Multi-agent collaboration — isolated Agents with shared artifacts](DRAFT-multi-agent.md) |  | agent-lifecycle | Each agent is its own pod and Agent resource, fully isolated by default, collaborating only through explicitly declared shared artifacts rather than shared workspaces. |
| DRAFT | [Notifications as a per-owner attention record; the agent still owns session truth](DRAFT-notification-record.md) |  | agent-lifecycle | The platform stores what needs a user's attention, written by a lease-elected watcher pulling on pod notices, with seen and dismissed as per-user watermarks; the agent remains the sole source of truth for which sessions exist. |
