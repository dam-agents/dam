#!/usr/bin/env bash
#MISE description="Print the KUBECONFIG path for the cluster. Usage: export KUBECONFIG=\"$(mise run cluster:kubeconfig)\""
#MISE quiet=true
. "$MISE_CONFIG_ROOT/.mise/lib/cluster.sh"
echo "$KUBECONFIG"
