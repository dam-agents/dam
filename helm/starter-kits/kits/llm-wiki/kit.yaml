schemaVersion: v1
id: llm-wiki
name: LLM Wiki
description: >-
  A knowledge base agent that builds a structured wiki with a knowledge graph,
  from the llm-wiki toolkit. Point it at repos and documents, then ask it
  questions.
category: knowledge
icon: chart
# The template's bootstrap installs this command; the first session runs it.
onboarding: { command: wiki-onboard }
knowledgeBase:
  template: llm-wiki
# The toolkit is the workspace: seeded at the commit the catalog resolved, then
# its own bootstrap runs from that checkout — nothing is fetched at install.
seed:
  url: https://github.com/dam-agents/llm-wiki-v2
  ref: main
install:
  command: bash bootstrap.sh
providers: [ibm-litellm, anthropic, openai]
