[tasks.check]
depends = [":check:*"]

# Chart dependencies build into charts/ before any task runs; see .mise/deps.
[deps.helm]
auto = true
sources = ["Chart.yaml", "Chart.lock", ".mise/deps"]
outputs = ["charts/*.tgz"]
run = "./.mise/deps"
